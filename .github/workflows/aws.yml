name: my-project
on:

  push:
    branches:
      - main


permissions:
  id-token: write
  contents: read

jobs:
  build_and_deploy:
    strategy:
      matrix:
        module: [ 'Esign' ]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set Environment Variables
        run: |
          # Source the properties file to set environment variables
          source .github/workflows/properties.txt
          
          # Access the variables
          echo "ECR_REPOSITORY: $matrix.module_ECR_REPOSITORY"
          echo "DOCKERFILE_PATH: $matrix.module_DOCKERFILE_PATH"
          echo "ECS_SERVICE: $matrix.module_ECS_SERVICE"
          echo "CONTAINER_NAME: $matrix.module_CONTAINER_NAME"
          echo "ECS_TASK_DEFINITION: $matrix.module_ECS_TASK_DEFINITION"
   

          
